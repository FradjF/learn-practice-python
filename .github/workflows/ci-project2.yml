name: Project 2 CI

on:
  push:
    paths:
      - "proj02_expenseTracker/**"
      - ".github/workflows/ci-project2.yml"
  pull_request:
    paths:
      - "proj02_expenseTracker/**"
      - ".github/workflows/ci-project2.yml"

jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        working-directory: ./proj02_expenseTracker
        run: python -m pip install -r requirements-dev.txt

      - name: Run pytest
        working-directory: ./proj02_expenseTracker
        run: python -m pytest